import { HeaderDirective } from './header.directive';

describe('HeaderdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderDirective(null);
    expect(directive).toBeTruthy();
  });
});
